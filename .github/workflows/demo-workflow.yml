name: Demo Workflow

on:
  workflow_dispatch:
  schedule:
    - cron: "* 15 * * *"

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Hej BMW 🚙"
